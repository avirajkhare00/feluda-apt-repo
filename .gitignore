# APT repository generated files (but allow the main repository structure)
# dists/
# pool/
# db/
conf/options
conf/override.*

# Allow APT repository structure
!dists/
!pool/
!db/

# Temporary files
*.tmp
*.temp
# *.deb  # Allow .deb files in the repository
*.changes
*.dsc

# GPG files
*.gpg
*.asc
private.key

# Build artifacts (but allow the final package)
feluda_*_amd64/
!pool/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
